# name: Build a testy

# on:
#   push:
#   pull_request:
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   build:
#     name: Ověření distribučního souboru
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: npm
#       - run: npm ci
#       - run: npm run validate
#       - name: Kontrola aktuálního buildu
#         run: git diff --exit-code -- dist/custom-history-bar.js
