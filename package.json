{
  "name": "custom-history-bar",
  "version": "0.1.0",
  "description": "Configurable state history bar card for Home Assistant",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "dev": "vite --host 127.0.0.1",
    "test": "vitest run",
    "validate": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "happy-dom": "^20.10.6",
    "typescript": "^5.8.3",
    "vite": "^6.2.3",
    "vitest": "^3.0.9"
  }
}
